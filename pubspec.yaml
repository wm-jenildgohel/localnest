name: localnest
description: Local-only MCP server for secure project context retrieval across cloned repositories.
version: 0.1.0-beta.2
repository: https://github.com/wm-jenildgohel/localnest

environment:
  sdk: ^3.11.0

executables:
  localnest:

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
